<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xsi:schemaLocation="http://www.hazelcast.com/schema/config hazelcast-config-4.0.xsd"
           xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <!-- Import license key configuration -->
    <!-- not needed for this version of the lab
    <import resource="member-license-key.xml" />
    -->

    <network>
        <join>
            <!-- For trainings everyone should create his/her own cluster, therefore
            multicast is disabled and other nodes are only searched for on localhost -->
            <multicast enabled="false"/>
            <tcp-ip>
                <interface>127.0.0.1</interface>
            </tcp-ip>
        </join>
    </network>

    <map name="training">
        <map-store enabled="true">
            <class-name>com.hztraining.DummyMapStore</class-name>
        </map-store>

        <!-- Configure eviction for 5 elements per member -->


    </map>

</hazelcast>